import { Google } from 'react-bootstrap-icons';

export const Login = () => {
	return (
		<div id="login">
			<button className="outlined">
				<Google /> &nbsp; Continue with Google
			</button>
		</div>
	);
};
